// Stamped by esbuild's `define` at build time (see esbuild.config.mjs). Automated
// release builds (.github/workflows/release.yml) set DESKLEAF_RELEASE_DATE before
// building; local dev builds (bash deploy.sh) leave it unset.
declare const __DESKLEAF_RELEASE_DATE__: string;

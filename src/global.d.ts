// Stamped by esbuild.config.mjs from DESKLEAF_RELEASE_DATE at build time.
// "" for local/dev builds (bash deploy.sh) that don't run the release pipeline.
declare const __DESKLEAF_RELEASE_DATE__: string;
